
export enum Nodes {
    Devnet = 'https://api.lb-0.h.chrysalis-devnet.iota.cafe/',
    Mainnet = 'https://chrysalis-nodes.iota.org/',
}

export enum Networks {
    Devnet = 'devnet',
    Mainnet = 'mainnet',
}

export enum Keys {
    Network = 'selectedNetwork',
    Devnet = 'devnetAddressList',
    Mainnet = 'mainnetAddressList',
    Unit = 'selectedUnit',
}

